pipeline {
agent any  
    tools {
        terraform 'terraform-11'
    }
    stages {
        stage('init') {
            steps{
                    
                        script {		
                                                          sh "terraform init"
							  sh "terraform fmt" 
							  sh "terraform plan"
							  sh "terraform apply --auto-approve"
                        }
	          }
		}
	  /*	stage('init') {
            steps{
                    dir('/opt/terraform/') {
                        script {		
                              sh "terraform init"
                        }
                      
                    }
	          }
		}
		
        stage('fmt') {
            steps{
                    dir('/opt/terraform/') {
                              sh "terraform fmt" 
                    }
	          }
		}
		
	stage('plan') {
            steps{
                    dir('/opt/terraform/') {	
                              sh "terraform plan"
                    }
	          }
		}
		
	stage('approve') {
            steps{
                    dir('/opt/terraform/') {
                              sh "terraform apply --auto-approve"
                      
                    }
	          }
		}*/
	}
}	
